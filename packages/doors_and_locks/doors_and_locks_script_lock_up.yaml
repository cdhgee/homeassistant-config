script:
  doors_and_locks_lock_up:
    alias: "Doors & locks - lock up"
    sequence:
    - alias: "Close the garage door"
      service: cover.close_cover
    - alias: "Lock the front door"
      service: lock.lock
      target:
        entity_id: lock.front_door_lock
