alias: "Close garage"

sequence:
  - conditions:
      - condition: state
        entity_id: cover.garage_door
        state: open
  - service: cover.close_cover
    target:
      entity_id: cover.garage_door
