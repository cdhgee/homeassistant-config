alias: "Lock up"

sequence:
  - parallel:
      - alias: Close the garage door
        service: script.close_garage
      - alias: Lock the front door
        service: lock.lock
        target:
          entity_id: lock.front_door_lock
      - alias: Cancel auto-lock timer
        service: timer.cancel
        target:
          entity_id: timer.auto_lock
