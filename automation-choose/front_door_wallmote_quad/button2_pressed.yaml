conditions:
  - condition: template
    value_template: "{{ action_name == 'KeyPressed' and property_key == '002'}}"
sequence:
  - service: cover.close_cover
    target:
      entity_id: cover.garage_door
