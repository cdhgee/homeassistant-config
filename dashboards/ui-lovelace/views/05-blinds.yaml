icon: mdi:window-open
title: Blinds
path: blinds
badges: []
type: panel
cards:
  - type: custom:auto-entities
    card:
      type: custom:layout-card
      layout_type: masonry
      layout:
        max_cols: 4
    filter:
      include:
        - group: cover.all_blinds
          options:
            type: custom:button-card
            entity_id: this.entity_id
            template:
              - blind
    sort:
      method: name
