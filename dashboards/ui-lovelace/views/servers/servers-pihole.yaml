icon: mdi:server
title: Servers - Pi-hole
path: servers-pihole
type: panel
subview: true
badges: []
cards:
  - type: custom:decluttering-card
    template: two-grid
    variables:
      - type: outer
      - cards:
          - type: custom:decluttering-card
            template: auto-entities-buttons
            variables:
              - filter-criteria: entity_id
              - filter-value: /^(switch|sensor)\\\\.pihole_a/
              - button-template: switch
          - type: custom:decluttering-card
            template: auto-entities-graph
            variables:
              - filter-criteria: entity_id
              - filter-value: /^(switch|sensor)\\\\.pihole_b/
