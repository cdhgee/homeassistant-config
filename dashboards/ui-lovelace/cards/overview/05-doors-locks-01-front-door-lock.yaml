type: custom:button-card
template:
  - dashboard-tile
  - state-tile
  - binary-entity
  - tap-service
entity: lock.front_door_lock
tap_action:
  service: >
    [[[ return (entity.state == "locked") ? "lock.unlock" : "lock.lock"; ]]]
