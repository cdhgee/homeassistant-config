type: custom:auto-entities
card:
  type: vertical-stack
  title: Family room
card_param: cards
filter:
  include:
    - group: light.family_room_lights
      options:
        type: custom:button-card
        entity_id: this.entity_id
        template:
          - dashboard-lights-tile
