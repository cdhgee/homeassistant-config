type: custom:auto-entities
card:
  type: vertical-stack
  name: Kitchen
card_param: cards
filter:
  include:
    - group: light.kitchen_lights
      options:
        type: custom:button-card
        entity_id: this.entity_id
        template:
          - dashboard-lights-tile
