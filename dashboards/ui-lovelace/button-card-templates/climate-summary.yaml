climate-summary:
  template:
    - base
    - label-tile
  label: >
    [[[
      switch(entity.state){
        case "off":
          return "Off";
          break;

        case "idle":
          return entity.state[0].toUpperCase() +
          entity.state.substr(1).toLowerCase() +
          " set to " +
          entity.attributes.temperature +
          ", " +
          entity.attributes.hvac_action;
          break;

        default:
          return entity.hvac_action[0].toUpperCase() +
          entity.hvac_action.substr(1).toLowerCase() +
          " to " +
          entity.attributes.temperature;
          break;
      }
    ]]]
  styles:
    icon:
      - color: >
          [[[
            if(entity.attributes.hvac_action != "idle") return "var(--secondary-background-color)";
            else return "var(--light-primary-color)";
          ]]]
    label:
      - color: >
          [[[
            if(entity.attributes.hvac_action != "idle") return "var(--secondary-background-color)";
            else return "var(--light-primary-color)";
          ]]]
    name:
      - color: >
          [[[
            if(entity.attributes.hvac_action != "idle") return "var(--secondary-background-color)";
            else return "var(--light-primary-color)";
          ]]]
    card:
      - background-color: >
          [[[
            if(entity.attributes.hvac_action != "idle") return "var(--light-primary-color)";
            else return "var(--ha-card-background)";
          ]]]
