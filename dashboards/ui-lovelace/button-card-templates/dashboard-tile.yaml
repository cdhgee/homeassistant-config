dashboard-tile:
  tap_action:
    action: navigate
  hold_action:
    action: more-info
  show_label: true
  layout: icon_name_state2nd
  aspect_ratio: 4/1
  styles:
    name:
      - font-size: var(--paper-font-body1_-_font-size)
    state:
      - font-size: var(--paper-font-body1_-_font-size)
    label:
      - font-size: var(--paper-font-body1_-_font-size)
    icon:
      - color: var(--paper-item-icon-color)
