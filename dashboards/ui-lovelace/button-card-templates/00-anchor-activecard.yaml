00-anchor-activecard:
  styles: &activecard
    name:
      - color: var(--secondary-background-color)
    state:
      - color: var(--secondary-background-color)
    label:
      - color: var(--secondary-background-color)
    icon:
      - color: var(--secondary-background-color)
    card:
      - background-color: var(--light-primary-color)
