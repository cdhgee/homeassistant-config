binary-entity:
  state:
    - value: on|open|unlocked
      operator: regex
      styles:
        state:
          - color: var(--paper-item-icon-active-color)
        label:
          - color: var(--paper-item-icon-active-color)
  tap_action:
    action: toggle
