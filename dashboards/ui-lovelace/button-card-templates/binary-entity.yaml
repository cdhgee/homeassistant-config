binary-entity:
  state:
    - value: on|open|unlocked
      operator: regex
      styles:
        icon:
          - color: var(--paper-item-icon-active-color)
        state:
          - color: var(--paper-item-icon-active-color)
        label:
          - color: var(--paper-item-icon-active-color)
  tap_action:
    action: toggle
