binary-entity:
  state:
    - operator: template
      value: >
        [[[ entity.state in ("on", "open") ]]]
      styles:
        state:
          - color: var(--paper-item-icon-active-color)
        label:
          - color: var(--paper-item-icon-active-color)
  tap_action:
    action: toggle
