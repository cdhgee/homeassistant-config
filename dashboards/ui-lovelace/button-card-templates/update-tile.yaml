update-tile:
  show_state: false
  show_label: true
  label: >
    [[[
      return (entity.state === 0 || entity.state === false || entity.state === "off") ? "Up-to-date" : "Update available";
    ]]]
  tap_action:
    action: navigate
