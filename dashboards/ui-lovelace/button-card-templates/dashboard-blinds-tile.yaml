dashboard-blinds-tile:
  template: dashboard-tile
  label: >
    [[[ return (entity.state == 0) ? "Closed" : entity.state + " on";]]]
  state:
    - value: 0
      styles:
        icon:
          - color: "var(--paper-item-icon-color, #44739e)"
    - operator: default
      styles:
        icon:
          - color: "var(--paper-item-icon-active-color, #fdd835)"
