conditions:
  - condition: and
    conditions:
      - condition: trigger
        id: morning
      - condition: state
        entity_id: binary_sensor.workday
        state: "on"

sequence:
  - service: scene.turn_on
    target:
      entity_id: scene.morning_weekday
